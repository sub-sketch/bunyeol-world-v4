# 『분열된 세계』 v4 — R6 집중패스 패치노트
> 2026-08-16 · 집중패스_실행계획서.md P1~P4 실행 결과
> 최종 빌드: `분열된세계_v4\빌드\분열된세계_v4_R6_집중패스.html`

---

## 한 줄 요약
**모션이 더 이상 튀지 않고(P1), 때리는 맛이 생겼고(P2), 런마다 빌드를 고르는 재미가 붙었습니다(P3).**

---

## P1. 모션 규격화 — 캐릭터 크기 펌핑 근본 수리

### 무엇이 문제였나
대기·걷기·공격·사망 시트마다 **PNG 캔버스 높이가 제각각**이었습니다. 게임 코드는 이미지
전체 높이를 그대로 "발밑 기준 박스"로 쓰기 때문에(앵커=하단 중앙), 상태가 바뀔 때마다
캐릭터가 순간적으로 커졌다 작아졌다 했습니다.

실측 결과 — 30종 전부 불일치:

| 캐릭터 | 수리 전 셀 높이 | 수리 후 |
|---|---|---|
| knight | 48 / 52 / 58 (3종) | **60 단일** |
| wight | 52 / 61 / 76 / 85 / 88 (5종!) | **68 단일** |
| ghoul | 44 / 45 / 47 | **53 단일** |
| dk | 62 / 65 | **74 단일** |

### 어떻게 고쳤나
`tools/normalize_sheets.py` 신규 — 캐릭터별로

1. `idle_s`(정면 대기)의 알파 콘텐츠 높이 = 그 캐릭터의 TARGET
2. 모든 프레임의 알파 bbox만 추출 (**리스케일 없음** — 배율 왜곡 0)
3. 캐릭터 공통 고정 셀 `(maxW+4, max(TARGET+12, 최대콘텐츠높이))` 에
   **발끝=셀 하단 / 가로 중앙** 으로 재배치
4. 원본은 `*_prenorm.bak` 로 백업 후 같은 파일명으로 저장

프레임별 재정규화는 하지 않습니다 — 그게 원래 사고의 원인이었습니다.
캐릭터 하나당 스케일은 단 하나입니다.

### 결과
- 검증 스크립트 **30/30 PASS** (`python tools/normalize_sheets.py --verify`)
- 런타임 실측: idle/walk/attack/death 의 렌더 높이가 전부 동일 → **전환 시 크기 변화 0px**
- 검증 갤러리: `모션검수_갤러리_v4_R6a.html` (대기→걷기→공격 연속재생 GIF 30종)
- 되돌리기: `python tools/normalize_sheets.py --restore`

---

## P2. 타격감 폴리싱

수치만 조정했습니다. 새 시스템은 추가하지 않았습니다.

| 항목 | 내용 |
|---|---|
| 히트스톱 | 타격 성공 시 **60ms** 전역 프리즈. 스킬로 마무리하면 **90ms** |
| 피격 넉백 | 몹이 플레이어 반대 방향으로 **3px** 밀렸다 0.1초에 걸쳐 복귀 |
| 사망 연출 | 파티클 12→**24**, 0.05초 뒤 2차 스파크, 사망 첫 프레임 **0.12초 홀드** |
| 데미지 숫자 | 처형·특효·베는 눈은 **1.4배로 팝** 후 0.15초에 걸쳐 축소 |
| 공격 선딜 | 내려치기 예비 동작(첫 프레임) **0.09초 홀드** |
| 화면 흔들림 | 일반 타격 2.4→**2.0** (절제), **킬 순간만 3.2** |

### 작업 중 발견한 잠재 버그 2건 (함께 수리)
1. **몹 사망 애니메이션이 한 번도 재생된 적이 없었음** — `killMob()` 이 `m.deathT` 를
   기록하지 않았고, 렌더는 `m.dead` 를 보자마자 그리기를 건너뛰었습니다. 즉 4프레임짜리
   사망 시트가 통째로 사장돼 있었습니다. 사망 연출 항목이 실제로 동작하려면 필수라 함께
   고쳤습니다(사망 애니메이션 재생 시간 동안 계속 그리도록).
2. **몹 피격 넉백이 실제 좌표를 밀고 있었음** — 누적되면 몹 위치가 조금씩 흔들렸습니다.
   플레이어와 같은 방식(렌더 시점 시각 전용, `mobKnock`)으로 바꿔 좌표는 건드리지 않습니다.

---

## P3. 계시(啓示) 시스템 — 런 중 문신 빌드 ★신규

### 컨셉
층을 클리어하면 신탁이 **계시 3택**을 내리고, 고른 계시가 **문신으로 몸에 새겨집니다.**
런 한정 — **죽으면 사라집니다.**

**진행 순서가 바뀌었습니다:**
```
층 전멸 → [계시 3택 · 빌드] → [물자 2택 · 보급] → [다음 층 워프]
            ↑ 신규, 먼저 뜬다
```

### 계시 12종 (3계열 × 4종)

**빛 — 유지·회복 계열**
| 이름 | 효과 |
|---|---|
| 「스스로 아무는 살」 | HP 재생 +60% |
| 「빛의 장막」 | 층 진입 보호막 +20 (메타 쉴드 노드와 합산) |
| 「승리의 성찬」 | 처치 시 HP 3 회복 |
| 「자비의 유예」 | 치명상 1회 무효 후 HP 25% (런 1회, 불굴과 **별개**) |

**검신(斬) — 공격 계열**
| 이름 | 효과 |
|---|---|
| 「벼려진 칼끝」 | 공격력 +4 |
| 「바람의 손목」 | 공격 속도 +12% |
| 「목을 베는 눈」 | HP 30% 이하 적 피해 +40% (처형 노드와 합산) |
| 「이어지는 참격」 | 처치 후 2초간 공격력 +6 (갱신형) |

**무신(舞) — 기동·회피 계열**
| 이름 | 효과 |
|---|---|
| 「그림자 걸음」 | 이동 속도 +10% |
| 「스치는 죽음」 | 회피 쿨타임 -30% |
| 「회피 반격」 | 회피 직후 1.5초간 공격력 +8 |
| 「가시 문신」 | 피격 시 공격자에게 피해 6 반사 |

**심화**: 같은 계시가 다시 나오면 「심화」 표기 — 효과 **1.5배**로 갱신.
**최대 2단**까지만(무한 스택 없음), 2단에 도달한 계시는 이후 후보에서 제외됩니다.

### 시각 연출
- **새기는 순간** — 계열색 스파크 + 발광 링이 몸에서 퍼짐
- **HUD** — 버프 아이콘줄에 문신 아이콘이 런 내내 상주 (계열색 테두리, 호버 시 효과 설명)
- **캐릭터** — 문신 2개마다 계열색 미세 발광 오버레이 (alpha 0.15, 가장 많이 새긴 계열의 색)
- **등급** — 문신 3개마다 이능 등급 표기가 한 단계 짙어짐 `최하급 (하급)`
  *(HUD 괄호 표기만 — 실제 레벨·저장 데이터는 건드리지 않습니다)*

### 사망 시 소멸
계시 상태는 전부 `RUN` 안에만 있고 `RUN` 은 **저장되지 않습니다.** 정산을 닫는 순간
`RUN = null` 이 되어 문신·효과·HUD 아이콘·등급 표기가 한꺼번에 사라집니다.
구조적으로 보장되는 것이라 따로 정리 코드가 필요 없습니다.

---

## P4. 통합 검증 · 수급 조정

### 수급 조정
- 층 보상 **「전리품 자루」 은화 140 → 120** (-14%)
  계시가 층당 메인 보상이 되었으므로 물자 쪽 비중을 낮췄습니다. 은화는 런 안에서만
  쓰는 자원이고(정산 때 0이 됩니다) 상인·제단이 주 소비처입니다.
  → **체감이 빡빡하면 `src/24_run.js` 의 `gold` 항목에서 120 두 곳만 140 으로 되돌리면 원복됩니다.**

### 회귀 검증 (Playwright, 전 구간 자동)
새 캐릭터 생성 → 스탯 8P 배분 → 런 시작 → 1층 전멸 → 계시 3택 → 물자 2택 → 2층 워프 →
상인·제단 → 문신 4개 누적 → 사망 → 정산(레벨·장비 초기화) → 메타 구매 → 재런 →
5층 보스 클리어 → 클리어 정산

**FAIL 0건 / 페이지 오류 0건**

주요 확인 항목: 사망 시 레벨 12→1·장비 longsw→시작무기·가방 속 미착용 예비 방어구 제거,
완료 퀘스트·처치 기록·기록물 보존, 메타 보너스 유지, 1부 클리어 영구 기록(META.clear1).

---

## 검증 스크립트 (소스 폴더에서 실행)

| 스크립트 | 대상 | 결과 |
|---|---|---|
| `python tools/normalize_sheets.py --verify` | P1 시트 규격 | 30/30 PASS |
| `node verify_r6a_normalize.js` | P1 런타임 프레임 높이 | PASS |
| `node verify_r6b_combat.js` | P2 히트스톱·넉백·사망연출 | PASS |
| `node verify_r6c_revelation.js` | P3 계시 12종 수치·순서·소멸 | 43/43 PASS |
| `node verify_r6_regression.js` | P4 전 기능 회귀 | PASS |

---

## ⚠ 사용자 확인이 필요한 항목 (샌드박스에서 검증 불가)

아래는 격리 환경에서 확인할 수 없어 **대표님이 실기에서 직접 봐 주셔야 하는 것들**입니다.
"테스트 전부 통과"에 포함되지 않습니다.

1. **타격감 체감** — 히트스톱 60ms·선딜 0.09초·흔들림 2.0 이 실제로 손에 어떻게 붙는지는
   수치 검증으로 알 수 없습니다. 과하거나 답답하면 각 항목이 독립적이라 하나씩 되돌릴 수 있습니다.
2. **은화 120 하향 체감** — 상인·제단에서 살 것을 제대로 못 사는 느낌이면 140으로 원복하십시오.
3. **문신 발광 세기** — alpha 0.15 로 잡았습니다. 모니터·밝기에 따라 안 보이거나 과할 수 있습니다.
4. **모바일 실기** — 계시 3택 창은 기존 `frewov` 를 재사용하므로 `#wrap` 축소 이슈에서
   자유롭지 않을 수 있습니다. 작은 폰에서 버튼 3개가 제대로 눌리는지 확인 요망.
5. **Windows 실행 / .bat 진입점** — 컨테이너에서 확인 불가.
6. **한 런에서 문신은 최대 4개** — 보상 층이 1~4층뿐이라(5층은 보스, 정산이 보상) 등급 표기는
   실제로 `하급`까지만 올라갑니다. 6개(중급) 이상을 보이게 하려면 보상 층을 늘리거나
   문신 획득 경로(상인·제단 등)를 추가해야 합니다 — 설계 판단이 필요해 손대지 않았습니다.

---

## 변경된 파일

**신규**
- `data/revelations.json` — 계시 12종 데이터
- `src/24c_revelation.js` — 계시 로직·UI·시각 연출
- `tools/normalize_sheets.py` — 모션 규격화 도구
- `verify_r6a_normalize.js` / `verify_r6b_combat.js` / `verify_r6c_revelation.js` / `verify_r6_regression.js`

**수정**
- `assets/pc/*` (8개) · `assets/mob/*/*` (218개) — 규격화 (원본은 `*_prenorm.bak`)
- `src/02c_pcsheet.js` — mobKnock 추가, 공격 첫 프레임 홀드
- `src/02d_mobsheet.js` — 사망 첫 프레임 홀드
- `src/05_sound.js` — hitstopT 전역
- `src/07_state.js` — pMaxHit/pAtkMs/pMS 에 계시 가산
- `src/14_combat.js` — 히트스톱·처형(계시)·성찬·연쇄·자비·가시·사망연출
- `src/14c_hazard.js` — 회피 쿨 감소·회피 반격
- `src/15_skills.js` — 스킬 킬 히트스톱·흔들림 절제
- `src/18_update.js` — 히트스톱 시간 동결·HP 재생 가산
- `src/19_render.js` — 몹 사망 렌더 유지·방향성 넉백·데미지 숫자 팝·계시 발광
- `src/20_hud.js` — 문신 아이콘(계열색)·등급 임시 표기
- `src/24_run.js` — RUN.revs 초기화·보호막 합산·계시 체인·은화 140→120
- `src/_order.json` · `build.py` · `shell/template.html`
